# Manary - Amplify Chart Edition

Manary is a chart-management workspace for midwifery clinics. This edition mirrors the chart experience from the Supabase-backed project, but all breast-care and postpartum-care charts are stored locally in the browser via `localStorage`. Administrator authentication still runs on Amplify Auth (Amazon Cognito), and the UI remains a Next.js App Router application that deploys cleanly to AWS Amplify Hosting.

## Highlights
- Amplify Auth sign-in with the Amplify UI `Authenticator` component (Cognito under the hood).
- Local storage data model – charts live inside the browser (`manary-free-charts-state`), so no database is required after sign-in.
- Recurring-billing-aware dashboard – subscriptions stay active only while the backend reports a valid payment state.
- Built-in chart templates that reproduce the Supabase reference layouts for quick previews or printing.
- Static-friendly build output ready for Amplify Hosting without custom scripting.

## Tech Stack
- Next.js 15 (App Router) + React 19
- AWS Amplify Auth + Amplify UI (`@aws-amplify/ui-react`)
- Tailwind CSS + shadcn/ui components
- TypeScript 5

## Directory Layout
```
manary-free/
├── amplify_outputs.json         # Generated by Amplify CLI (`amplify pull`)
├── app/
│   ├── api/                     # Next.js Route Handlers (subscription verifier, auth helpers)
│   ├── dashboard/               # Auth-protected chart dashboard
│   ├── product/                 # Subscription and payment landing page
│   ├── layout.tsx               # Root layout + global providers
│   └── page.tsx                 # Landing page with Amplify Auth entry point
├── components/
│   ├── charts/                  # Chart forms, details, diagrams, manager
│   ├── subscription/            # Trial banner, payment form, unlock form
│   └── providers/               # Amplify + chart state providers
├── data/                        # Development billing fixtures
├── lib/                         # Library utilities (Amplify, encryption, billing client)
├── public/                      # Static assets
└── styles/                      # Tailwind styles
```

## Amplify Auth Setup (follow the [AWS docs](https://docs.amplify.aws/react/build-a-backend/auth/))
1. **Install the Amplify CLI (Gen 2)** – `npm install -g @aws-amplify/cli`.
2. **Initialize the backend** – inside the repository run `amplify init` (or `amplify sandbox`) and choose the defaults for a JavaScript app.
3. **Add authentication** – run `amplify add auth` and choose **Default configuration** (email sign-in is a good default).
4. **Push the backend** – run `amplify push` (or `amplify deploy`) to create Cognito resources in AWS.
5. **Pull the outputs into the app** – `amplify pull --appId <appId> --envName <env>` (or accept the prompt after `amplify push`). This generates/updates `amplify_outputs.json`.
6. **Commit or securely store `amplify_outputs.json`** – the app imports this file at runtime. Replace the placeholder file checked into the repo with the real one generated by Amplify.

> The placeholder `amplify_outputs.json` in the repository only keeps TypeScript happy. Replace every `"REPLACE_ME"` value with the real data supplied by the Amplify CLI.

## Local Development
```bash
npm install
npm run dev
# visit http://localhost:3000
```

Ensure `amplify_outputs.json` is populated before starting the dev server; otherwise the Amplify Auth SDK cannot connect to your user pool.

### Environment variables

| Variable | Required | Description |
| --- | --- | --- |
| `NEXT_PUBLIC_UNLOCK_CODE` | ✔︎ | Magic unlock code used by the demo card form and to seal the local encryption key. Keep it unique per environment. |
| `BILLING_SERVICE_URL` | – | Base URL of a billing backend that can validate subscription status. When omitted, the app falls back to the local fixture in `data/subscriptions-dev.json`. |
| `BILLING_SERVICE_API_KEY` | – | Optional bearer token sent to the billing backend. |

Create a `.env.local` file during development (values shown for illustration only):

```bash
NEXT_PUBLIC_UNLOCK_CODE=DEV-UNLOCK-123
BILLING_SERVICE_URL=https://billing.example.com/api
# BILLING_SERVICE_API_KEY=sk_live_...
```

## Sign-in Flow Summary
1. `/` renders contextual information along with the Amplify `Authenticator` sign-in experience.
2. After a successful sign-in, the user is redirected to `/dashboard`.
3. Clicking **Sign out** calls `Authenticator`’s `signOut`, clears the Amplify session, and returns to `/`.

## Subscription lifecycle

| State | Source | What users see | How to recover |
| --- | --- | --- | --- |
| `trial` | Auto-generated on first launch. | Trial banner with remaining days; charts remain unlocked. | Click the banner to open the `/product` page and complete payment. |
| `expired` | Trial duration elapsed or billing backend reports `inactive`. | Dashboard redirects to `/product`, encrypted chart store is locked. | Fix payment status, then use the unlock form with the latest code. |
| `paid` | Billing backend reports an active subscription. | Full dashboard access and encrypted charts are restored automatically. | – |

- The client polls `/api/subscription/verify` on load and every hour to keep status fresh.
- The verify endpoint delegates to `lib/server/billing-client.ts`, which either calls `BILLING_SERVICE_URL` or loads `data/subscriptions-dev.json`.
- When billing reports a lapse, the local encryption key is discarded to protect the stored charts until payment resumes.

### Local billing fixture

The repository includes `data/subscriptions-dev.json` for offline development:

```json
[
  {
    "unlockCode": "MANARY-UNLOCK-CODE",
    "status": "active",
    "trialEndsAt": "2025-11-10T00:00:00.000Z",
    "currentPeriodEndsAt": "2099-12-31T00:00:00.000Z",
    "lastPaymentStatus": "paid",
    "message": "Using local billing fixture."
  }
]
```

Add or edit entries to emulate different billing responses. In production, remove the fixture from the build or replace it with environment-managed data.

## Local Storage Schema
Chart data lives under `localStorage["manary-free-charts-state"]` and looks like:

```json
{
  "charts": [
    {
      "id": "uuid",
      "chartType": "breast",
      "patientName": "Hanako Yamada",
      "patientId": "BC-0001",
      "visitDate": "2025-10-12",
      "practitionerName": "Jun Sato",
      "createdAt": "2025-10-12T09:30:00.000Z",
      "updatedAt": "2025-10-12T09:45:00.000Z",
      "data": {
        "breastMilkInterval": "Every 3 hours",
        "nippleAreolaCondition": ["Mild fissure"],
        "careDetails": "Expressed milk 20 ml",
        "initialConsultationFee": true
      }
    }
  ]
}
```

Use your browser dev tools (Application → Storage → Local Storage) to back up or clear the chart data.

## Amplify Hosting Deployment
1. Connect the repository in Amplify Hosting (GitHub or manual).
2. Build settings:
   ```bash
   npm install
   npm run build
   ```
   Output directory: `out`
3. Copy your production `amplify_outputs.json` into the build environment (for example via the Amplify console’s file management or by storing it in the repo if acceptable).
4. Redeploy after replacing the placeholder outputs file.

### Manual Publish (Amplify CLI)
```bash
npm install -g @aws-amplify/cli
amplify init
amplify add auth
amplify push
npm install && npm run build
amplify publish              # uploads the ./out directory
```

## Troubleshooting
- **Authenticator loops back to `/`** – ensure `amplify_outputs.json` matches the environment you deployed with `amplify pull`.
- **Sign-in succeeds but the dashboard is blank** – make sure the authenticated user exists in the Cognito user pool created by Amplify.
- **Charts disappear after refresh** – the browser stores `localStorage` per domain/device; export or copy `manary-free-charts-state` before clearing storage.
- **Billing status never updates** – confirm the unlock code exists in your billing backend (or the local fixture) and that `BILLING_SERVICE_URL` is reachable from the browser.

---

Feel free to open an issue if you spot a bug or want to contribute improvements.
